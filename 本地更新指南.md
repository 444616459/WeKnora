
# WeKnora 本地更新指南

## 第一步：同步代码

1. 访问 GitHub 仓库：[https://github.com/444616459/WeKnora](https://github.com/444616459/WeKnora)
2. 点击 **"Sync fork"** 按钮将上游仓库的更新同步到你的 fork
3. 切换到 `dev` 分支
4. 将 `main` 分支的更新内容合并到当前分支

## 第二步：构建镜像

### 构建所有镜像

```bash
sh scripts/build_images.sh
```

### 或构建特定镜像

```bash
sh scripts/build_images.sh -p  # 只构建后端
sh scripts/build_images.sh -f  # 只构建前端
```

## 第三步：启动服务

### 启动不同功能组合

根据项目需求，选择以下启动方式之一：

- **启动最小功能**
  ```bash
  docker compose up -d
  ```

- **启动全部功能**
  ```bash
  docker-compose --profile full up -d
  ```

- **启用链路追踪（Jaeger）**
  ```bash
  docker-compose --profile jaeger up -d
  ```

- **启用 Neo4j 知识图谱**
  ```bash
  docker-compose --profile neo4j up -d
  ```

- **启用 MinIO 文件存储服务**
  ```bash
  docker-compose --profile minio up -d
  ```

- **多选项组合启动**
  ```bash
  docker-compose --profile neo4j --profile minio up -d
  ```

### 停止服务

```bash
./scripts/start_all.sh --stop
# 或
make stop-all
```

## 🌐 服务访问地址

启动成功后，可通过以下地址访问服务：

| 服务 | 地址 |
|------|------|
| Web UI | `http://localhost` |
| 后端 API | `http://localhost:8080` |
| 链路追踪（Jaeger） | `http://localhost:16686` |

## 故障排除

如果遇到问题，请参考以下解决方案：

- 确保 Docker 和 Docker Compose 已正确安装并运行
- 检查本地端口是否被其他服务占用
- 查看 Docker 容器日志以获取错误详情